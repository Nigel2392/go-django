version: "2"
sql:
  - schema: "./schema.sql"
    queries: "./queries.sql"
    engine: "mysql"
    gen:
      go:
        package: "models"
        out: "../auth-models"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        query_parameter_limit: 8
